server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Application React (SPA) : toute route côté client (ex: /dashboard)
    # doit renvoyer index.html, sinon nginx cherche un fichier physique
    # inexistant et répond 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
