route:
  receiver: slack-default
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 30m

receivers:
  - name: slack-default
    webhook_configs:
      - url: http://localhost:9999/alert-sink
        send_resolved: true
